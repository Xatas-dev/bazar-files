quarkus:
  datasource:
    username: postgres
    password: postgres
    jdbc:
      url: jdbc:postgresql://localhost:5432/bazar_file

  s3:
    endpoint-override: http://localhost:9000
    path-style-access: true
    aws:
      region: us-east-1
      credentials:
        type: static
        static-provider:
          access-key-id: test
          secret-access-key: test123

settings:
  s3:
    bucket:
      files: files
    url-ttl: 3600